fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
pytest>=8.1.0
pytest-asyncio>=0.23.0
websockets>=12.0
twilio>=8.0
python-multipart>=0.0.9
stripe>=10.0.0
# zoneinfo reads the OS tz database, which Linux has and Windows does not.
# Pinning it here keeps the phone agent's "today is …" identical on Render and
# on a dev machine instead of raising ZoneInfoNotFoundError on one of them.
tzdata>=2024.1
